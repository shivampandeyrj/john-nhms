# Code Conventions

## Frontend
- Vanilla DOM manipulation (`document.getElementById`, `addEventListener`)
- Standard HTML5 semantic structures
- CSS custom properties for theming (`var(--color-teal)`)

## Backend
- ES6 syntax in Apps Script (`Code.gs`)
- Basic try/catch error handling in `doPost`
- JSON ContentService responses
